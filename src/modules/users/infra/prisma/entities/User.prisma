model Users {
  id         String   @id @default(uuid())
  name       String  
  birthday   String
  email      String
  cpf        String   @unique
  phone      String
  password   String
  created_at DateTime @default(now())
  updated_at DateTime @default(now())

  @@map("users")
}
